import { StubPage } from '@/components/stub-page';

export default function Page() {
  return (
    <StubPage
      title="Workers"
      description="Worker portal: assigned tasks, brief downloads, uploads, productivity."
      taskRef="docs/01 FR-D5"
    />
  );
}
